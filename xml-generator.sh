# This script accepts a list of URLs (links.txt) and generates an xml favourites import for major web browsers
# Use the learning resources.md to select which links you would like access to and edit down the `links.txt` file in this repo
